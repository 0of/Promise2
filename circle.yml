general:
  build_dir: test

machine:
  pre:
    - sudo apt-get install g++-5
    - sudo update-alternatives --install /usr/bin/g++ g++ /usr/bin/g++-5 20

test:
  override:
    - cmake .
    - make all
    - make run_test